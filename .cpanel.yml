---
deployment:
  tasks:
    - /bin/bash -lc 'for p in /opt/cpanel/ea-nodejs*/bin/node /opt/alt/alt-nodejs*/root/usr/bin/node /usr/local/bin/node /usr/bin/node; do if test -x "$p"; then printf "%s " "$p"; "$p" --version; fi; done > /home/ontimegrpup/deployment-runtime.txt 2>&1; ls -l /etc/cpanel/ea4/passenger.nodejs* >> /home/ontimegrpup/deployment-runtime.txt 2>&1 || true'
