---
deployment:
  tasks:
    - /bin/bash -lc 'for p in /opt/cpanel/ea-nodejs*/bin/node; do if test -x "$p"; then printf "%s " "$p"; "$p" --version; fi; done > /home/ontimegrpup/deployment-runtime.txt 2>&1'
